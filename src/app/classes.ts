// create an interface to represent our session:
export class SessionStorageModel {
    UserId?:Number = -1;
    // could add more fields if we wanted to
    
    // TODO: how could we store data for a user who isn't logged in
}